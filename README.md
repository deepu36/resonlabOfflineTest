# Reason Labs Offline test

## Install node modules

npm install

## Development server

To run Curd application execute below command
`npm run startCurdApplication`

To pizza order application execute below command
`npm run startOrder`
